{
  "name": "default",
  "version": "1.0.0",
  "private": true,
  "description": "Default Gask starter project",
  "author": "",
  "keywords": [],
  "main": "index.js",
  "scripts": {
    "build": "gask build",
    "dev": "gask dev --push",
    "deploy:dev": "gask build --push dev",
    "deploy": "gask build --push prod",
    "gask": "gask",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^6.6.1",
    "@types/google-apps-script": "^2.0.8",
    "eslint": "^9.39.1",
    "gask": "^1.0.0-beta.10"
  }
}
